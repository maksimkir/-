1)static string Popav(int x, int y, int R) // Метод, який перевіряє, чи потрапляє точка у певну область.

2) double d = x * x + y * y; // Обчислення квадрата відстані від початку координат до точки.
   if (Math.Abs(d - R * R) < 1e-9) return "At the boundary"; // Якщо точка на межі кола (з точністю до 1e-9), повертає "At the boundary".
   if (d < R * R) return "Yes"; // Якщо точка всередині кола, повертає "Yes".

3)if (x >= 0 && y >= 0) // Якщо точка у першій чверті (x ≥ 0, y ≥ 0)